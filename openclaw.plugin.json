{
  "id": "xiaoyi-router",
  "name": "Xiaoyi Router",
  "description": "Xiaoyi local routing proxy for OpenClaw",
  "version": "0.2.0",
  "main": "./dist/index.js",
  "activation": {
    "onStartup": true
  },
  "configSchema": {
    "type": "object",
    "description": "Provide either config or configPath. port, upstreamUrl, and trace override config.proxy.* at runtime.",
    "properties": {
      "config": {
        "type": "object",
        "description": "Inline RawConfig passed directly to xiaoyi-router."
      },
      "configPath": {
        "type": "string",
        "description": "Path to a RawConfig JSON file for xiaoyi-router."
      },
      "port": {
        "type": "number",
        "default": 8402,
        "description": "Runtime override config.proxy.port."
      },
      "upstreamUrl": {
        "type": "string",
        "default": "https://api.deepseek.com",
        "description": "Runtime override config.proxy.upstreamUrl."
      },
      "trace": {
        "type": "string",
        "enum": ["off", "summary", "debug"],
        "description": "Runtime override config.proxy.trace."
      }
    }
  }
}
