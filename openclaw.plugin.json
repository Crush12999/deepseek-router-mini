{
  "id": "xiaoyi-router",
  "name": "Xiaoyi Router",
  "description": "Xiaoyi local routing proxy for OpenClaw",
  "version": "0.1.0",
  "main": "./dist/index.js",
  "activation": {
    "onStartup": true
  },
  "configSchema": {
    "type": "object",
    "properties": {
      "port": {
        "type": "number",
        "default": 8402,
        "description": "Local proxy port. Can also be set with XIAOYI_ROUTER_PORT."
      },
      "upstreamUrl": {
        "type": "string",
        "default": "https://api.deepseek.com",
        "description": "DeepSeek-compatible upstream API base URL. Can also be set with XIAOYI_BASE_URL."
      }
    }
  }
}
